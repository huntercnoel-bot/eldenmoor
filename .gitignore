# Local server data (contains hashed account passwords — keep out of the repo)
accounts.json

# Python / OS junk
__pycache__/
*.pyc
.DS_Store

# Archives
*.zip

# Agent / Playwright verification scratch (drive scripts + screenshots)
*.mjs
/*.png

# Subagent git worktrees
.claude/worktrees/
